import { type PropsWithChildren } from 'react';

const {{pascalCase name}} = ({ children }: PropsWithChildren) => {
  return (
    <div>
      <h1>{{pascalCase name}} Component</h1>
      {children}
    </div>
  );
}

export default {{pascalCase name}};
